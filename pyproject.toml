[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "witnessed"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["coverage>=7.10", "pytest"]

[project.scripts]
witnessed = "witnessed.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["witnessed*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
